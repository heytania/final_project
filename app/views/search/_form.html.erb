<%= search_form_for(@query, url: search_path, method: :get, class:'d-flex') do |f| %>
    <%= f.search_field :brand_or_name_i_cont_any, placeholder: "Search..." %>
    <%= f.submit "Search", class:"btn btn-outline-info" %>
<% end %>